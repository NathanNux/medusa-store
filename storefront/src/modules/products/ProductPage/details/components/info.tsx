const InfoDesc = ({ description }: { description: string }) => {
    return (
        <div className="info">
            <p>{description}</p>
        </div>
    )
}


export default InfoDesc

// WIP: Create later text animations for it, for all of the 3 components. so it flows